// Packages
import styled from "styled-components"
import { Grid, Variables } from "components-react-julseb"

// Styles
const ListCards = styled(Grid)`
    gap: ${Variables.Spacers.M};
`

export default ListCards
