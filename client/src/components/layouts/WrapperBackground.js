// Packages
import styled from "styled-components"
import { Wrapper, Variables } from "components-react-julseb"

// Styles
const WrapperBackground = styled(Wrapper)`
    padding-top: calc(${Variables.Spacers.XXL} + 64px);
    min-height: 95vh;
`

export default WrapperBackground
